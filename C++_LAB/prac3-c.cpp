#include <iostream>
using namespace std;

int main(){
    int matrix[3][3];
    int counter;

    cout<<"enter 10 number : "<<endl;
    for(counter = 0 ; counter < 10 ; counter++)
    {
        cin>>numbers[counter];
    }
    int largest;
    largest = number[0];
    cout<<"the array element are"<<endl;
    for(counter = 0 ; counter <10; counter++);

    {
        cin>>matrix[row][col];

    }
    cout<<"the matrix element are "<<endl;
    for(row = 0;row<3;row++)
    {
        for(col = 0;col<3;col++){
            if(row ==col){
                cout<<matrix[row[col]]<<"/t";
                
            }
        }

    }
    return 0;
}